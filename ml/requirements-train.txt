# Core training env for train_all.py (MSTAR + YOLO + BIT simple + Triton export)
# Python 3.10–3.12. Install PyTorch with CUDA first if pip picks CPU-only wheels:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.2.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.3.0
Pillow>=10.0.0
PyYAML>=6.0
tqdm>=4.66.0
pandas>=2.0.0
httpx>=0.27.0
kaggle>=1.6.0
onnx>=1.15.0
python-dotenv>=1.0.0

# Optional: full open-cd BIT trainer (Linux + Python 3.10 recommended)
# pip install -r ml/requirements-train-opencd.txt
